import 'package:flutter/material.dart';

class LearningReminders extends StatelessWidget {
  const LearningReminders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
