import 'package:flutter/material.dart';

class CareerGoal extends StatelessWidget {
  const CareerGoal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
