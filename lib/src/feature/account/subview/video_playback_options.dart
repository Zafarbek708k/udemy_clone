import 'package:flutter/material.dart';
class VideoPlaybackOptions extends StatelessWidget {
  const VideoPlaybackOptions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
